fn inner f fn(i64) -> i64 v i64 -> i64
    f(v)

fn outer f fn(i64) -> i64 v i64 -> i64
    inner(f, v)

fn main -> i64
    let n i64 = 2
    let c fn(i64) -> i64 = fn x i64 -> x + n
    outer(c, 40)
