fn main -> i64
    let m map<string, i64> = map_new()
    let keys list<string> = map_keys(m)
    let vals list<i64> = map_values(m)
    # An empty map yields empty key/value lists, so this loop never runs.
    for i from 0 to len(keys) - 1
        println(get(keys, i))
    len(keys) + len(vals)
